apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: jenkins

resources:
  - namespace.yaml
  - local-pv.yaml       # 동적 프로비저너가 있으면 이 줄 삭제
  - rbac.yaml
  - casc.yaml
  - jenkins.yaml
  - ingress.yaml
  # DNS/Ingress 없이 노드 IP 로 접속 (http://<노드IP>:30808)
  - nodeport.yaml
