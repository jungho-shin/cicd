# NOTE: patch 대상 매칭용 파일이므로 metadata.namespace 를 지정하지 않는다.
# upstream install.yaml 의 리소스에는 namespace 가 없고, kustomization 의
# `namespace: argocd` 트랜스포머는 patch 적용 이후에 실행되기 때문이다.
# GitLab webhook 검증용 시크릿을 argocd-secret 에 병합한다.
# GitLab 리포지토리 Settings > Webhooks 의 Secret token 과 같은 값을 넣는다.
apiVersion: v1
kind: Secret
metadata:
  name: argocd-secret
type: Opaque
stringData:
  webhook.gitlab.secret: __REPLACE_ME__
